Download the attached jar file and execute the following command from the commandline (cmd.exe)
   java -jar routRep.jar routes.txt fare_attributes.txt fare_rules.txt 
Observe that the report/output files(route_types.txt and route_cost.txt) are created in the same directory. 